---
layout: default
---
{% if page._styles %}
  <!-- Page/Post style -->
  <style type="text/css">
    {{ page._styles }}
  </style>
{% endif %}

<div class="post">
  <header class="post-header">
    <h1 class="post-title">
      {%- if page.title_ko -%}
        <span class="lang-en">{{ page.title }}</span><span class="lang-ko">{{ page.title_ko }}</span>
      {%- else -%}
        {{ page.title }}
      {%- endif -%}
    </h1>
    <p class="post-description">
      {%- if page.description_ko -%}
        <span class="lang-en">{{ page.description }}</span><span class="lang-ko">{{ page.description_ko }}</span>
      {%- else -%}
        {{ page.description }}
      {%- endif -%}
    </p>
  </header>

  <article>
    {{ content }}
  </article>

  {% if page.related_publications %}
    <h2>References</h2>
    <div class="publications">
      {% bibliography --cited_in_order %}
    </div>
  {% endif %}

  {% if site.giscus and page.giscus_comments %}
    {% include giscus.liquid %}
  {% endif %}
</div>
